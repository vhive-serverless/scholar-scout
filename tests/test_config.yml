---
email:
  username: ${GMAIL_USERNAME}
  password: ${GMAIL_APP_PASSWORD}
  folder: "Google Scholar Alerts"

slack:
  api_token: ${SLACK_API_TOKEN}
  default_channel: "#scholar-scout-test"
  channel_topics:
    scholar-scout-llm: ["LLM Inference"]
    scholar-scout-serverless: ["Serverless Computing"]

perplexity:
  api_key: ${PPLX_API_KEY}
  model: "sonar-pro"

research_topics:
  - name: "LLM Inference"
    keywords: ["llm", "inference", "serving", "latency", "throughput"]
    slack_users:
      - "@test1"
      - "@test2"
    slack_channel: "#scholar-scout-llm"
    description: "Test LLM inference research"

  - name: "Serverless Computing"
    keywords: ["serverless", "faas", "function-as-a-service", "cloud"]
    slack_users:
      - "@test3"
      - "@test4"
    slack_channel: "#scholar-scout-serverless"
    description: "Test serverless computing research"